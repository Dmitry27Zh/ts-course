"use strict";
const brandRecord = {
    apple: {
        country: 'USA',
        name: 'Apple',
        createdAt: new Date(),
    },
};
function printPcCatalog(pcCatalog) {
    var _a;
    console.log((_a = pcCatalog.dell) === null || _a === void 0 ? void 0 : _a.name);
}
const p = {
    screen: window.screen,
};
